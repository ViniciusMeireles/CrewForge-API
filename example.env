# PostrgreSQL
POSTGRES_DB=crew_forge_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=postgres_db
POSTGRES_PORT=5432

# Django API
ENVIRONMENT=local_development
DJANGO_WSGI_MODULE=config.wsgi
ALLOWED_HOSTS=*
DJANGO_SECRET_KEY==j)cewcxc(3tedwf@(9c#o@jw)x-iq=gmr17mt4qf)nyzh!_fi
DJANGO_SETTINGS_MODULE=config.settings.local
DEBUG=True
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
PYTHONUNBUFFERED=1
STATIC_ROOT=/staticfiles
MEDIA_ROOT=/media/
MEDIA_URL=/media/
GUNICORN_WORKERS=3
GUNICORN_THREADS=2
GUNICORN_TIMEOUT=120
GUNICORN_LOG_LEVEL=info
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:5173

# SameSite: None (default) > front-end and API on different domains
#           Lax          > same domain or subdomains
# SESSION_COOKIE_SAMESITE=None
# CSRF_COOKIE_SAMESITE=None

# Shared session cookie domain for subdomains (e.g. .example.com)
# SESSION_COOKIE_DOMAIN=.example.com

# Trusted origins for CSRF (required for cross-origin POST requests)
# CSRF_TRUSTED_ORIGINS=https://app.example.com

## System
SYSTEM_TITLE=CrewForge

## Email configuration
DEFAULT_FROM_EMAIL=crew_forge <noreply@crew_forge.com>
SERVER_EMAIL=CrewForge <noreply@crew_forge.com>
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=mailpit
EMAIL_PORT=1025
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
EMAIL_HOST_USER=crew_forge@crew_forge.com
FROM_MAIL=CrewForge <noreply@crew_forge.com>
EMAIL_HOST_PASSWORD=password
## Simple JWT
ACCESS_TOKEN_LIFETIME=10080
REFRESH_TOKEN_LIFETIME=10080

START_API=False

# Frontend
FRONTEND_URL=http://localhost:5173
FRONTEND_RESET_URL=http://localhost:5173/reset-password

# API
SELF_URL=http://localhost:8000
